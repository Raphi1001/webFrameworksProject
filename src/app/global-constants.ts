export class GlobalConstants {
    public static token: number = 100000;
    public static currentUser: {
        email:string, adress: string, city: string, postalCode:number
    } = {
        email: "test", adress: "test", city: "test", postalCode: 5
    }
}